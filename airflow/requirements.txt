# =============================================================================
# Providers y dependencias para Apache Airflow 2.9.x
# =============================================================================

# Providers de Airflow
apache-airflow-providers-apache-spark==4.7.1
apache-airflow-providers-mongo==3.5.0
apache-airflow-providers-postgres==5.10.2

# Clientes de bases de datos
psycopg2-binary==2.9.9
pymongo==4.6.1

# PySpark (versión debe coincidir con la imagen apache/spark:3.5.1)
pyspark==3.5.1
